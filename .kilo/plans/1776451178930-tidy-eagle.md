# Database User Query Plan

## Goal
Check the Supabase database to see what users are currently created in the system.

## Context
The user has multiple projects with different Supabase databases:
- his-lite-backend: Uses `https://eh3u9azfs5nadmuqrvfw.supabase.co`
- mi-proyecto: Uses `https://qoylngqedifnahaoezdv.supabase.co`
- his-app: Appears to be using Next.js with missing .env (likely same as one of the above)

## Steps

### 1. Identify Target Database
- Determine which project/database the user wants to query
- Check which environment variables are active/being used
- Confirm which Supabase URL and anon key to use

### 2. Establish Database Connection
- Use Supabase dashboard or SQL editor to connect
- Verify credentials are correct and have proper permissions
- Access the database via Supabase web interface

### 3. Query User Data
- Query the `profiles` table (based on schema.sql) to see user profiles
- Query the `auth.users` table to see auth users (if accessible)
- Check for any clinics/tenants if applicable
- Look for role assignments and clinic associations

### 4. Verify Schema Consistency
- Confirm the `profiles` table exists and has the expected columns
- Check if the `create_user_profile` RPC function is deployed
- Verify any foreign key constraints (clinics table)

### 5. Document Findings
- List all existing users with their details
- Note any discrepancies between local schema and deployed database
- Identify any data integrity issues

## Tools Needed
- Supabase dashboard access
- SQL queries via Supabase SQL editor
- Environment variable verification

## Expected Outcome
Clear view of what users exist in the database, helping diagnose authentication issues.